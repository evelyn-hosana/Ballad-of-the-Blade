//Create Event
// Set default visibility to false
// Set default visibility to false
visible = false;
notes = [];  // Store added notes
depth = 0;  // Higher value places it further back

// Store initial sprite dimensions
sheet_width = sprite_get_width(spr_sheetMusic);
sheet_height = sprite_get_height(spr_sheetMusic);
