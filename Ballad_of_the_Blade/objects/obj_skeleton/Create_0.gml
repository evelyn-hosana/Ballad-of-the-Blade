/// Create Event of obj_enemy

hspd = 3; // horizontal speed
vspd = 0;   
state = "walk"; // current state (e.g., "walk", "dead")
image_speed = 0.2; // how fast to animate the sprite

// Ensure the sprite is the walk sprite initially
sprite_index = spr_skeleton;